# Python
__pycache__/
*.pyc
venv/
.venv/
build/
dist/
*.spec

# Local data
logs/
config.json
.kilo/

# OS
.DS_Store
Thumbs.db

# Models are NOT committed to git (GitHub 100MB limit + LFS disabled on fork).
# They ship inside the portable EXE and the NSIS installer (--add-data).
# Users get them automatically; sources documented in models/README.md.
models/**/*.onnx
models/voices/*.pth
*.onnx.q8.onnx
